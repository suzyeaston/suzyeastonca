.env
.env.*
!.env.example
.env.deploy.local
wp-config.php
local-config.php
secrets.php
*.pem
*.key
.DS_Store
screenshots/
playwright-report/
test-results/

# Private resume tooling and exports (local only — not in public repo)
private-resume/
/build/*.zip

# Generated release binaries are shared as artifacts, not committed.
/dist/*.zip
